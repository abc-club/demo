(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(require('echarts/lib/echarts'), require('echarts/lib/chart/line'), require('echarts/lib/component/tooltip'), require('echarts/lib/component/title'), require('echarts/lib/component/toolbox')) :
    typeof define === 'function' && define.amd ? define(['echarts/lib/echarts', 'echarts/lib/chart/line', 'echarts/lib/component/tooltip', 'echarts/lib/component/title', 'echarts/lib/component/toolbox'], factory) :
    (global = global || self, factory(global.echarts));
}(this, (function (echarts) { 'use strict';

    // 引入 echarts 主模块。

    console.log(echarts);
    // 基于准备好的dom，初始化 echarts 实例并绘制图表。
    echarts.init(document.getElementById('main')).setOption({
        title: {text: 'Line Chart'},
        tooltip: {},
        toolbox: {
            feature: {
                dataView: {},
                saveAsImage: {
                    pixelRatio: 2
                },
                restore: {}
            }
        },
        xAxis: {},
        yAxis: {},
        series: [{
            type: 'line',
            smooth: true,
            data: [[12, 5], [24, 20], [36, 36], [48, 10], [60, 10], [72, 20]]
        }]
    });

})));
//# sourceMappingURL=line.min.js.map
